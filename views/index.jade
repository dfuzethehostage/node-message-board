extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new") send message
  p text: #{messages[messages.length-1].text} 
  p user: #{messages[messages.length-1].user} 
  p added: #{messages[messages.length-1].added}